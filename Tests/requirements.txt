# Python testing requirements for Arch Linux installation script
# Install with: pip install -r requirements.txt

# Core testing framework (built-in, but listed for completeness)
# unittest - Built into Python standard library

# Additional testing utilities (optional)
pytest>=7.0.0
pytest-html>=3.1.0
pytest-cov>=4.0.0

# JSON handling (built-in, but useful for reports)
# json - Built into Python standard library

# Path handling (built-in)
# pathlib - Built into Python standard library

# Subprocess management (built-in)
# subprocess - Built into Python standard library

# Regular expressions (built-in)
# re - Built into Python standard library

# Type hints (built-in in Python 3.5+)
# typing - Built into Python standard library
